// for clearing local storage

const clearLocalStorage = () => {
  localStorage.removeItem('miningNumber');
};

// Call clearLocalStorage() where appropriate, for example when the component mounts:
useEffect(() => {
  clearLocalStorage();
}, []);
